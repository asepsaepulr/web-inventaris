apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "web-inventaris.fullname" . }}-test-connection"
  labels:
    {{- include "web-inventaris.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "web-inventaris.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
