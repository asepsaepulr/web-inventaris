apiVersion: v1
kind: Service
metadata:
  name: {{ include "web-inventaris.fullname" . }}
  labels:
    {{- include "web-inventaris.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "web-inventaris.selectorLabels" . | nindent 4 }}
